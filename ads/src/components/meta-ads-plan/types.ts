import type {
  MetaAdsPlan as Plan,
  Campaign,
  Ad,
  AdAssignment,
  AdSetConfig,
  AdRun,
  SpendDay,
} from '../../data/interfaces';

export type { Plan, Campaign, Ad, AdAssignment, AdSetConfig, AdRun, SpendDay };
